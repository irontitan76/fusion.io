{
  "name": "fusion.io",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cd ./client && npm run-script lib && npm run-script build && cd ./../server && npm run-script lib",
    "build_start": "npm run build && npm run start",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "start": "cd ./server && npm run-script start",
    "test": "echo /",
    "install_": "npm install && cd ./client && npm install && cd ./../server && npm install"
  }
}
